/*'use strict';

let localConfig = {
    hostname: 'server',
    port: 3000,
    viewDir: './app/views'
		
};

module.exports = localConfig;
*/

'use strict';

let localConfig = {
    hostname: 'localhost',
    port: 3000,
    viewDir: './app/views'
		
};

module.exports = localConfig;
